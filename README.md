# Fabric Packages

Official maintained package implementations built on the public APIs of
[`Onoal/fabric`](https://github.com/Onoal/fabric). The Fabric kernel does not
know these package semantics: first- and third-party packages use the same
extension machinery. The initial family is `KeyValueStore`, with memory and
filesystem realizations.
