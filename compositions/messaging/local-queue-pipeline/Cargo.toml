[package]
name = "onoal-fabric-composition-local-queue-pipeline"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Reusable local queue pipeline Composition for Fabric."

[lib]
name = "fabric_composition_local_queue_pipeline"
path = "src/lib.rs"

[dependencies]
fabric.workspace = true
fabric-package-messaging-queue.workspace = true

[dev-dependencies]
futures.workspace = true
